# Email Generator

Generate email drafts for common business workflows.

## Prompt

Uses prompt registry entry: `email-generator` (`v1`).

## CLI example

```bash
byo-llm run email-generator "Write a follow-up email after a job interview" --tone warm --audience "hiring manager" --purpose "thank them and restate fit"
```

## Sample dataset

See `sample-requests.json` for sample request + parameter combinations.
